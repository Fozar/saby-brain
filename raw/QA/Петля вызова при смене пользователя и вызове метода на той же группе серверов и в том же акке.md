---
title: "Петля вызова при смене пользователя и вызове метода на той же группе серверов и в том же акке."
source: "https://link.sbis.ru/page/forum-card/c1eea789-a574-4a9d-96ea-53bf14ddee0a"
author:
published:
created: 2026-07-22
description:
tags:
  - "clippings"
---
ТА

22

ср

wasaby.Backend

![[02532d42f47621addcf630e122acaf72_MD5.avif]]

Разговоров Андрей

Календарь и планировщик

Петля вызова при смене пользователя и вызове метода на той же группе серверов и в том же акке.

05.08.25

Есть метод, в который передаётся другой пользователь. Внутри происходит вызов метода с сессией переданного пользователя на той же группе серверов и в том же аккаунте, что и текущий пользователь. В результате образуется петля вызова. Как её избежать?

Ответы

1

Бадаев Евгений

:

Разговоров А.

Оценить

5 авг'25

Андрей, используйте такой рецепт:

![[10671913693748aff0ddd78f4ab4b13b_MD5.avif]]

Как вызвать синхронно БЛ-метод под другим клиентом и юзером, не образуя петли? 10.07.25

Комментарии

Бадаев Евгений

Сервер приложения

Оценить

05.08.25

Вам нужно использовать [CreateMultitenantEndpointByClientId](https://wi.sbis.ru/docs/py/multitenancy/methods/CreateMultitenantEndpointByClientId?v=25.2100).

Разговоров Андрей

:

Бадаев Е.

Оценить

05.08.25

Я так понимаю, так как у меня клиент тот же и сервис тот же, то мне не надо передавать внутрь service\_name.

А вот что делать с auth\_data? Мне нужен вызов под сессией другого пользователя. Откуда сессию Endpoint будет брать?

Вроде сейчас могу прокидывать auth\_data, только в конструктор Endpoint/

![[d0b1a1d090a19d801608dc5637e37b4d_MD5.avif]]

class sbis.EndPoint | Платформа СБИС

Бадаев Евгений

:

Разговоров А.

Оценить

05.08.25

Андрей, CreateMultitenantEndpointByClientId для этого и предназначен.

Разговоров Андрей

:

Бадаев Е.

Оценить

05.08.25

Евгений, Так сессию то пользователя мне как прокинуть созданный endpoint? Мне нужна сессия определённого пользователя, чтобы вызываемый метод внутри правильно права посчитал.

Бадаев Евгений

:

Разговоров А.

Оценить

05.08.25

Андрей, используйте такой рецепт:

![[10671913693748aff0ddd78f4ab4b13b_MD5.avif]]

Как вызвать синхронно БЛ-метод под другим клиентом и юзером, не образуя петли? 10.07.25

25

5

Сообщение...

Похожие темы

Тимошенко Александр

08.07.25

Как вызвать метод БЛ в другой схеме того же сервиса без петли вызовов?

Есть декоратор, который при обращении участника группы соцсети дергает тот же метод под владельцем группы. Например, вызов идёт из ext-registration в ext-registration, но под другим ClientID. Используем TenantContext + EndPoint('online', auth\_data). Получаем петлю вызовов. \[Цитата кода\] Уже задавали вопрос на эту тему и ошибка вроде бы даже исчезла, но начала повторяться в апреле 2025. Профилирование с петлей. Что уже пробовали: TenantContext, но петля остаётся. Что нужно: способ

Кузаков Юрий

13.06.17

Ошибка при вызове метода Записать

Возникает такая ошибка: Traceback (most recent call last): File "RetailPriceList.Update", line 4, in \_\_RetailPriceList\_Update\_1 from.wrap import \* File "/opt/sbis/ online.sbis.ru /service/Модули/Ценообразование/priceentity/retail/pricelist/update.py", line 13, in update result = sbis.PriceList.Записать(record) File "PriceList.Записать", line 3, in \_\_PriceList\_Записать sbis.error.Error: Внутренняя ошибка сервера. Попробуйте выполнить операцию позднее. \[1\] (ОПИСАНИЕ: Ошибка при

Носков Павел

21.01.22

Определить ОС при вызове метода БЛ

Как при вызове БЛ метода прочего сервиса, на питоне определить с какой ОС произошел вызов. Интересуют варианты: IOS, Android, Desktop Требуется, что бы отдать нужный образ дисконтной карты (Apple wallet или Google Pay)

Животов Сергей

03.02.23

Ошибка при вызове метода sbis.RpcFile.Data()

Иногда на тестовом стенде возникает ошибка при вызове метода rpcfile.Data() sbis.error.Error: Внутренняя ошибка сервера. Попробуйте выполнить операцию позднее. \[-1\] (ОПИСАНИЕ: RpcFile::GetData: Unable to read content of the RpcFile\[size=38826\] from stream\[eof=false, count=0\] into data buffer) с чем эта ошибка может быть связана?

Python

sbis

Киселев Алексей

11.10.24

Петли вызовов из-за заполнения Endpoint данными пользователя для проверки прав

Есть ошибка, которая говорит что была создана петля на тот же сервис. Для вызова метода, который проверяет метод БЛ на права без его вызова, использую код: """ check\_rights\_object, method\_availability = 'CheckRightsObject', 'MethodAvailability' end\_point = sbis.EndPoint( self.endpoint or 'online', auth\_data =sbis.AuthByClientUserID( sbis.Session.ClientID(), sbis.Session.UserID(), check\_rights = True, ) ) is\_right =

Python

права доступа

...

Емелин Илья

02.10.18

Почему при вызове метода появляется ошибка?

Индекс 60 выходит за пределы количества полей записи: 60. Если скопировать sql из метода - все отрабатывает корректно. Sql метода во вложении.

Лапицкий Роман

22.05.20

Почему возникает ошибка при RPC вызове метода?

При попытке вызвать метод модуля через стандартный RPC-механизм, получаю следующую ошибку. Метод объявлен в orx и корректно имплементирован в С++ коде с двумя входными параметрами.

Михайлов Виктор

20.11.24

Ошибка на локальном стенде при асинхронном вызове метода БЛ

На локальном стенде при попытке вызова метода БЛ через AsyncInvoke падает ошибка с текстом "(ОПИСАНИЕ: Ошибка при асинхронном вызове метода "<объект>.<метод>" с сервиса "<сервис>": Невозможно определить транспортный протокол для совершения вызова, т. к. для сервиса не установлен транспортный протокол по умолчанию)". Можно ли ее устранить?

Михайленко Елена

14.02.24

Как установить таймаут на выполнение метода БЛ при внутрисервисном вызове? EndPoint.SetTimeout можно применять только к межсервисным вызовам? Почему?

https://wi.sbis.ru/docs/py/sbis/EndPoint/methods/SetTimeout/?v=24.1100 И как будет работать такой код: \[Цитата кода\] Или такой, при выполнении его в сервисе online: \[Цитата кода\] Время на какие операции/части процесса ограничит указанное в SetTimeout значение? PS. Я видела этот вопрос, но хотелось бы обсудить тему еще раз и прояснить некоторые дополнительные моменты. Из ответа там стало только понятно что Execution Timeout использовать не рекомендуется

таймаут

Комлева Александра

16.12.20

Области видимости при вызове метода с фильтром ScopesAreas

Есть метод бизнес - логики "A", метод включен в зону доступа (участок системы) "Z". Вызываем списочный метод "A" с параметром фильтра "ScopesAreas": \['Z'\]. Внутри метода получаем ограничения через cfg('rights.ourOrgs'). Работает, приходят выставленные ограничения с зоны Z. Вызываем метод B, а уже внутри метод A с параметром фильтра "ScopesAreas": \['Z'\]. Какие ограничения будут установлены внутри метода A в таком случае? (Метод B явно не включен в зону "Z", включен

Постнов Святослав

05.08.24

Как избавиться от петли вызовов в одном юните?

Хотим в зависимости от роли аккаунту в группе соцсети отдавать ему данные либо с его схемы, либо со схемы другого аккаунта. Для этого сделали декоратор, который, если выполнены некоторые условия, вызывает тот же метод БЛ на другом аккаунте через BLObject.Invoke с пробросом данных авторизации через EndPoint. В итоге с этой схемой получили ошибку петли вызовов. Тестирование утверждает, что можно выполнить некоторую проверку принадлежности клиентов к одному юниту, чтобы затем при необходимости сделать

Козадой Юрий

03.05.23

Как сделать метод на ОС доступным для вызова с DWC, но недоступным для вызова извне?

Методы для служебного пула, специально убранные под DWC, т.к. потенциально это тяжёлые и долгие методы - как помешать пользователям вызывать их вручную прямо из консоли, в основном пуле? Провоцируя, таким образом, нештатную нагрузку в облаке. Сейчас они Internal application contract, но это позволяет выполнить их с клиента.

Михайленко Елена

12.03.25

Как настроить защиту от частых вызовов метода и при этом не мешать автотестам с вызовами метода?

Для метода настроена " Frequent call protection". Чтобы предотвратить слишком частые вызовы от "злоумышленников". Но автотесты тоже выполняют частые вызовы (с другой целью). Можно ли как-то решить эту проблему?

БЛ

Хурамшин Артём

27.10.17

Многочисленные вызовы метода авторизации sbis.AuthBy...

Добрый день! Хотелось бы узнать на сколько правильно дергать методы аутентификации в большом количестве с одного сервиса.

Д

Дубина Дмитрий

29.08.18

Есть ли API для вызова методы с online извне?

Например я хочу написать некоторую тулзу, которая будет что-то делать в зависимости от моего количества задач, каким способом мне получить это значение?

Метелягин Александр

20.11.17

Ошибка "IndexError: basic\_string::substr" при вызове метода BLObject.Записать()

Добрый день. При вызове функции Update() объекта sbis.BLServiceObject, привязанному к таблице с NK получаю ошибку: File "Contragent.Update", line 3, in \_\_Contragent\_Update IndexError: basic\_string::substr При этом запись тех же данных через sbis.SqlQuery проходит успешно. Другие объекты \*.orx, привязанные к таблицам с PrimaryKey, успешно отрабатывают Update(). Вопрос: можно ли доработать Update для корректной работы c таблицами, содержащими Natural Keys Ниже коды Python,

Михайленко Елена

22.02.22

Есть ли ограничение на размер передаваемых параметров при вызове метода БЛ (с фронтенда или с бэкенда, частности - при межсервисном вызове)?

Если есть, то какое? В частности интересует передача массива UUID - есть ли ограничение на максимальную длину этого массива?

вызов

межсервисное взаимоделйствие

Болдырев Алексей

10.07.19

Ограничение вызова метода конкретным интерфейсным модулем

Не было ли мыслей сделать дополнительные уровни доступа к методам? А именно Internal + "Вызов доступен только внутри модуля БЛ" и Public + "Вызов доступен только из указанных интерфейсных модулей". Для чего: На мой взгляд, каждый модуль должен иметь некое API для остальных модулей, при этом скрывать (инкапсулировать) внутреннюю реализацию. Бывали случаи, когда человек не спрашивая ответственных, звал метод EmployeeCard.ReadCard, который вычитывает информацию для карточки сотрудника, только

Морозов Данил

21.09.22

Очередь и метод вызываемый с интерфейса. Можно ли повлиять на время пребывания вызова такого метода в очереди, выставить некий таймаут после которого вызов удаляется из очереди и метод падает? И если да, то как?

Чунижеков Сергей

08.09.17

Где обрезается результирующий RS при межсервисном вызове списочного метода?

Ситуация следующая: Из основного сервиса вызывается метод в стороннем сервисе (Manually Implemented List, через Proxy Method). В вызове передаётся Навигация, скажем, в 30 элементов на страницу. Метод стороннего сервиса может отдавать записей больше чем 30, и если метод вызывается в рамках того же сервиса, то всё хорошо. Но, эмпирическим путём было выяснено, что при межсервисном вызове платформа где-то обрезает результирующий RS вызываемого метода до того размера, который передан в Навигации

Чумакин Андрей

22.01.20

Почему возникает ошибка вызова метода из онлайна, под пользователем со стороннего сервиса?

ОПИСАНИЕ: Ошибка при асинхронном вызове метода "RemoteCertificate.GetPrivateKey" с сервиса "online": Не удалось получить маршрут для пользователя "005a0cdd": отсутствует один или более из обязательных атрибутов (gid или dbid) Этот же вызов с локального стенда подключённого к облаке, проходит успешно. https://online.sbis.ru/shared/disk/45da4ea7-69d0-47b7-847f-baeab4dd9497\_e5ee340f-73df-4a26-a5a7-c61c53d31ef7

Аникеев Александр

30.11.17

FutureInvoke генерирует исключение sbis.error.Error при этом вызова метода не происходит.

Вызов метода через FutureInvoke иногда возвращает sbis.error.Error: 500 Internal Server Error при этом вызова метода не происходит. В логах:

Курбатов Антон

11.10.24

Долгое выполнение асинхронного вызова и предупреждение "вызов перенаправлен" в логах

Добрый день. Помогите интерпретировать логи вызова ActivityFixation.SendFillScheduleNotification/1. Я правильно понимаю, что произошло следующее: В 10:55 произошёл асинхронный вызов метода на основном. Метод не исполнится, и вызов перенаправлялся на другую БЛ. Исполнился только в 11:05. Всё так? Я понимаю, что вызов асинхронный, но 6 минут задержки как-то подозрительно много. Это нормально? Почему "Асинхронный вызов перенаправлен на бизнес-логику версии" это предупреждение? Что-то пошло не так

асинхронный вызов

Михайленко Елена

26.09.21

Как сформировать значение параметра HRef для вызова метода FileFS.Upload?

Например, моя задача - сохранять изображения для дизайна дисконтных карт. Изображения должны быть доступны по ссылке публично, без аутентификации в СБИС (в частности, для сервисов Google) Как должен выглядеть мой вызов FileFS.Upload? По какому алгоритму формируется значение HRef для этого вызова? Как получить Url загруженного файла, в соответствии с моими требованиями. https://wi.sbis.ru/docs/bl/discount-cards/%D0%9A%D0%BB%D0%B8%D0%B5%D0%BD%D1%82%20%D0%A5%D0%A4%20RPC/FileFS/methods/Upload/?v=21

хранилище файлов

Литвинцева Мария

27.12.21

В списочном методе заданы вычисляемые поля, в вызовах метода Read с онлайна можно указать какой списочный метод использовать и получить те же самые вычисляемые поля. Как получить их в обработчиках метода Update?

Ерусланов Дмитрий

17.08.21

При удаленном вызове метода с online'а с аутентификацией по UserID, падает ВОС по таймауту.

Пытаюсь вызвать метод с online'а для этого использую такую конструкцию: ep = sbis.EndPoint(service\_name='online', auth\_data=sbis.AuthByUserID(self.user\_id)) account\_data = ep.НашРасчетныйСчет.Прочитать(flt.Get('Purse'), 'НашРасчетныйСчет.Список') Но падает ВОС по таймауту. Если вместо sbis.AuthByUserID(self.user\_id)) использовать sbis.AuthByClientID(self.client\_id) то все работает, но нужна именно аутентификация по пользователю. В чем может быть проблема?

Анисимов Егор

21.08.18

При вызове Proxy-метода получаем ошибку "Клиент.ВнешнийИдентификаторТекущегоКлиента" с сервиса "online": 404 Not Found

Создаю Proxy-метод на online.sbis.ru, который зовет Custom метод с сервиса IntegrationConfig. В методе используются вызовы вида: \[Цитата кода\] Когда мы зовем метод, получаем ошибку: \[Цитата кода\] Если зовем напрямую, все ок. Что изменить что бы работало? Чего не хватает?

Смирнов Андрей

13.05.22

Как обновить/перезаписать кэш вызова метода с определенными параметрами на online?

Нам нужно перезаписать кэш вызова метода с определенными параметрами на online. Т.е. если мы позовем sbis.ThemeManager.ReadTheme(Id=2), чтобы кэш обновился только для вызова с параметром Id=2. В инструкции описано как обновить на сервисе кэш через вызов метода с отключением чтения из кэша (SetHeader( L"X-Method-Cache-Control", L"no-cache" )). Делали согласно инструкции: https://wi.sbis.ru/doc/platform/developmentapl/service-development/service-contract/objects/blcache/#\_5 Попробовал сделать

online

Сиварёв Владимир

20.09.22

Как запретить вызов метода для роли пользователь системы?

Создал участок системы. Включил метод в участок системы с флагом Allowed. для роли "Пользователь системы" пометил участок как Excluded. Метод всё равно вызывается через консоль и возвращает результат. В настройках ini файла всё нормально.

права доступа

роль

Михайленко Елена

19.01.22

Как установить таймаут на выполнение метода БЛ при внутрисервисном вызове (при этом оставив опцию вызывать его и без таймаута)?

Из документации https://wi.sbis.ru/doc/platform/developmentapl/service-development/service-contract/objects/blmethods/timeout/#execution-timeout сложилось впечатление что нужный мне атрибут - Execution timeout. Но метод для переопределения величины таймаута SetTimeout влияет только на Call timeout. Все верно? Для чего: На странице отображается таблица с итогами. При этом в зависимости от содержимого БД и параметров отбора итоги могут рассчитываться ощутимое время. Хотелось бы в таком случае

таймаут